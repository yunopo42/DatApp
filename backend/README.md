# Backend

This directory is reserved for the FastAPI application. It will be scaffolded
in a dedicated step with typed configuration, clear module boundaries, linting,
and a minimal test before database or product features are introduced.

